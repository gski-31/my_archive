<% include partials/header %>
<div class="container">
    <div class="container">
        <div class="row bg-dark p-2 text-white">
            <h2><%= campground.name %></h2>
        </div>
        <div class="row bg-light">
            <div class="col-md-3 p-3">
                <img src="<%= campground.image %>" class="img-fluid mx-auto d-block" alt="<%= campground.name %>" />
            </div>
            <div class="col-md-9 p-3">
                <p><%= campground.description %></p>
                <p class="text-right text-info"><em><%= campground.location %></em></p>
            </div>
        </div>
        <div class="row bg-dark p-2">
            <a href="/campgrounds" class="btn btn-success m-1">Go Back</a>
            <a href="/campgrounds/<%= campground._id %>/edit" class="btn btn-warning m-1">Edit</a>
            <form action="/campgrounds/<%= campground._id %>?_method=DELETE" method="POST">
                <button class="btn btn-danger m-1">Delete</button>
            </form>
        </div>
    </div>
</div>
<% include partials/footer %>