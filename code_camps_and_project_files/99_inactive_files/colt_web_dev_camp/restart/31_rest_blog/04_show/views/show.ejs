<% include partials/header %>

<div class="ui main text container segment">
    <div class="ui huge header">Show Page</div>
    <div>
        <h4><%= blog.title %></h4>
        <img src="<%= blog.image %>" />
        <p><%= blog.date.toDateString() %></p>
        <p><%= blog.body %></p>
        <a href="/" class="ui inverted blue button">Back</a>
    </div>
</div>

<% include partials/footer %>